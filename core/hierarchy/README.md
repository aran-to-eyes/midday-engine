# core/hierarchy

Entity tree topology and transform propagation; deterministic tree order is the default ordering for watchers, sequences, hooks, test queries (Aurora D-4). Arrives at m0-scene-hierarchy.
