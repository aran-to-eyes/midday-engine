# Godot reference clone (437 MB, analysis distilled into GODOT_REFERENCE_SPEC.md)
godot/

# Agent/session artifacts
.claude/

# Build output (all CMake presets configure into build/<preset>)
build/

# TS toolchain content-hash cache (regenerable; never drift-gated)
.midday-cache/

# Local tooling & agent contract files (no agent traces in git)
.venv-tools/
node_modules/
AGENTS.md
CLAUDE.md
