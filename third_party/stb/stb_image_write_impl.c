/* third_party/stb/stb_image_write_impl.c -- the single stb_image_write
   implementation TU (upstream-documented usage: define
   STB_IMAGE_WRITE_IMPLEMENTATION in exactly one translation unit). Kept in
   third_party/ so the vendored header body stays out of first-party lint;
   recorded in LICENSES/manifest.json. */
#define STB_IMAGE_WRITE_IMPLEMENTATION
#include "stb_image_write.h"
