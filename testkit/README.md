# testkit

Input playback, tick control, state snapshots/asserts, golden frames, geo queries. fixtures/ holds negative-test corpora used by scripts and CI. `rhi_backends.h` is the RHI conformance corpus's shared backend registry (one process-wide device per backend, loud skip accounting).
